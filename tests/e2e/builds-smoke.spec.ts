import { expect, test } from "@playwright/test";
const build = { id: "build-1", displayName: "Legacy label", resource: { owner: { scope: "local" }, locator: "C:/builds/fixture" }, server: { owner: { scope: "local" }, locator: "C:/builds/fixture/llama-server.exe" }, classification: "custom", tools: [{ kind: "server", exists: true }], managedInferenceEligibility: "eligible", configuredModelIds: [], validationInProgress: false, warnings: [], failures: [] };
const broken = { ...build, id: "build-2", resource: { owner: { scope: "local" }, locator: "C:/builds/broken" }, tools: [] };
test("Builds renders each authoritative Build once using its folder and marks broken builds", async ({ page }) => {
  const calls: string[] = []; await page.route("**/api/**", (route) => { const path = new URL(route.request().url()).pathname; calls.push(path); if (path === "/api/builds") return route.fulfill({ json: { revision: "1", builds: [build, broken] } }); if (path === "/api/runtime") return route.fulfill({ json: { state: {}, routerState: { activeBuildId: build.id }, warnings: [] } }); return route.fulfill({ json: {} }); });
  await page.goto("/#builds"); await expect(page.getByRole("row", { name: /fixture/ })).toHaveCount(1); await expect(page.getByText("Legacy label")).toHaveCount(0); await expect(page.getByText("llama-server.exe not found (possibly broken build)")).toBeVisible(); await page.getByText("llama-server.exe not found (possibly broken build)").first().click(); expect(calls.some((path) => path.includes("capabilities"))).toBe(false);
});
